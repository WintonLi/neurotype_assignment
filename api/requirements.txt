# Deliberately empty. Add whatever you want: fastapi, flask, django,
# litestar, sqlalchemy, psycopg, pytest. Nothing here is a recommendation.
aiosqlite>=0.22.1
alembic>=1.19.1
fastapi>=0.141.1
sqlalchemy>=2.0.52
uvicorn>=0.52.3
